{
  "name": "repo",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "clean": "turbo run clean && pnpm clean:root",
    "clean:root": "rimraf .turbo node_modules"
  },
  "packageManager": "pnpm@10.18.2",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.5.8"
  }
}
