{
  "name": "repo",
  "version": "0.2.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "clean": "turbo run clean && pnpm clean:root",
    "clean:root": "rimraf .turbo node_modules",
    "generate:client": "encore-cgen --output=./apps/frontend/src/lib/client.ts"
  },
  "packageManager": "pnpm@10.18.3",
  "devDependencies": {
    "@repo/encore-cgen": "workspace:*",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.8"
  }
}
